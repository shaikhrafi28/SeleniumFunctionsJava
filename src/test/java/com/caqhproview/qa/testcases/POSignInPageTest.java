package com.caqhproview.qa.testcases;

import org.testng.annotations.Test;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.BeforeSuite;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.Parameters;
import org.openqa.selenium.WebDriver;
import org.testng.Assert;
import org.testng.annotations.AfterClass;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.AfterSuite;
import org.testng.annotations.AfterTest;
import org.testng.annotations.BeforeClass;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;



import selenium.functions.java.base.TestBaseSetup;


public class POSignInPageTest extends TestBaseSetup {
 /*   POSignInPage signinPage;
	POHomePage homePage;

	public POSignInPageTest() {
		super();
	}

	@BeforeTest
	@Parameters("browser")
	public void setUp(String browser) throws InterruptedException {
		initialization(browser);
		giveProviewUrl();
		Thread.sleep(5000);
		signinPage = new POSignInPage();
	}

	@Test(priority = 1)
	public void loginTest() {
	signinPage.login(prop.getProperty("POuname"), prop.getProperty("POpswd"));

	}
	
	//@AfterTest
	//public void tearDown(){
	//	driver.quit();
	//}

	*/
	
}
